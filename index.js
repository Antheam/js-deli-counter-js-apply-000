
// function takeANumber(line, name) {
// line.push(name);
// return `Welcome, ${name}. You are number ${line.length} in line.`;
// }
var i = 0
function takeANumer(line) {
  line.push(i++)
  
return `now serving ${i}